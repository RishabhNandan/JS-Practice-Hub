4.Write a program which enter any integer and if it is less than 5 than calculation and print its cube otherwise calculate and prints its square.
Solve:-let number = 4; 
if (number < 5) {
    let cube = number * number * number; 
    console.log("Cube:", cube);
} else {
    let square = number * number; 
    console.log("Square:", square);
}
